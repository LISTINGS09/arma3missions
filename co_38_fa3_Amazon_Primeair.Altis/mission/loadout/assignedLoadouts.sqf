// Either use (_faction in ["blu_f"]) or (side == "west") etc...
// Note - Always lower case!
if (_side == "west") then {
	#include "f_loadout_a3_west_fia.sqf"
};
if (_side == "ctrg") then {
	#include "f_loadout_a3_west_ctrg_arid.sqf"
};