// Either use (_faction in ["blu_f"]) or (side == "west") etc...
// Note - Always lower case!
if(_side == "guer") then {
	#include "f_loadout_a3_guer_aaf_pom.sqf"
};
if(_side == "west") then {
	#include "f_loadout_a3_west_semiarid.sqf"
};
